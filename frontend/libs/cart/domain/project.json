{
  "name": "cart-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/cart/domain/src",
  "prefix": "m-org-",
  "projectType": "library",
  "tags": ["scope:cart", "type:domain"],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/cart/domain/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/cart/domain/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/cart/domain/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/cart/domain/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
